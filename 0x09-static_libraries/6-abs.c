#include "main.h"
/**
 * _abs - a function that give an absolute of anumber
 * @n: takes an integer type of functions
 * Return: 0 (success)
 */
int _abs(int n)
{
if (n < 0)
n = (-1) * n;
return (n);
}
